# LeadForge AI — Core Requirements for Local Development (no Docker needed)
# Run: pip install -r requirements-local.txt

# FastAPI & ASGI
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Database — SQLite for local dev (no PostgreSQL server needed)
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0
alembic==1.14.0

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1

# HTTP & Scraping (lightweight — no Playwright)
httpx==0.28.1
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0

# Data validation
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0

# Export
openpyxl==3.1.5
pandas==2.2.3

# Utilities
python-slugify==8.0.4
aiofiles==24.1.0
tenacity==9.0.0
structlog==24.4.0

# Rate limiting
slowapi==0.1.9
